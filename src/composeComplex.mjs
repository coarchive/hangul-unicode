import { complex } from './characters';

export const hasCounterpart = char => Object.prototype.hasOwnProperty.call(complex, char);
export { hasCounterpart };
export default ((first, second) => {

});
